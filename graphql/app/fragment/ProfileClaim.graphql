fragment ProfileClaim on ProfileClaim {
  profileId
  tenantId
  residentId
  actualResidentId
  profileStatus
  permissions
  email
  displayName
  tenantName
  modules {
    key
    name
    defaultIconKey
    ordinal
    tools {
      key
      name
      defaultIconKey
      route
      ordinal
    }
  }
}
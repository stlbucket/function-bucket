query SearchProfiles(
  $searchTerm: String
) {
  searchProfiles(_options: {
    searchTerm: $searchTerm
  }) {
   nodes {
      id
    	email
      firstName
      lastName
      fullName
      status
    }    
  }
}
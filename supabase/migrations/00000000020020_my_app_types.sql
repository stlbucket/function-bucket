-----------------------------------------------
-- script  my_app_fn schema types
--   it's good to put them here so you can run my_app_fn.sql by itself if you like via psql command line
-----------------------------------------------

-- create type my_app_fn.TYPE_NAME as (

-- );
